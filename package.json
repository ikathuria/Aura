{
  "name": "aura",
  "workspaces": ["frontend"], 
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "check:function-auth": "node scripts/check-supabase-function-auth.mjs",
    "validate:ci": "npm run lint && npm run typecheck && npm run check:function-auth && npm run build"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
